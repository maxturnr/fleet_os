# Same Supabase project as Pitch DMS ("pitch", eu-north-1)
NEXT_PUBLIC_SUPABASE_URL=https://qdkniarpazjwjsyamlpg.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Server-only: used for receipt uploads to the shared "invoices" bucket
SUPABASE_SERVICE_ROLE_KEY=
# AI assistant + receipt extraction
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-4-5
# Optional: set to ".pitchdms.com" in production so the auth cookie is shared with the DMS
AUTH_COOKIE_DOMAIN=
# Link back to the DMS for things Pitch Money doesn't do (deals, invoices)
NEXT_PUBLIC_DMS_URL=https://app.pitchdms.com
